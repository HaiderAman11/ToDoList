# 📝 To-Do List Console App (Python)

A basic to-do list app you can run in your terminal to manage your tasks.

## Features
- View tasks
- Add new tasks
- Delete tasks
- Saves data in `tasks.txt` file

## How to Run
1. Save the file as `todo_list.py`
2. Run it using:

```bash
python todo_list.py
